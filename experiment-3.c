#include <stdio.h>

int main() {
    int a, b, sum;
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("Sum = %d", sum);
    return 0;
}

//Flowchart:
/*Start → Input a,b → sum=a+b → Print sum → End*/

